# Baseline CI state — 2026-09-23

Recorded for Phase 0 (backlog `d01`, task 7). Repository: `RyanDam/zmk`
(fork of `zmkfirmware/zmk`).

## Current tracking branch: `feat/esp32` @ `4ef325f5b800e12e6c4e498d7df06ce1662a64f5`

- Pushed 2026-09-23; **no Actions run was triggered** because every workflow
  filters on `paths:` (`app/**`, `.github/workflows/*`, …) and the Phase 0
  commits touch only `esp-port/`.
- Manual dispatch is not possible without repo-scoped GitHub credentials.
- The authoritative local baseline for this branch state is
  `tests-2026-09-23.md` and `build-sizes-2026-09-23.md` in this directory.

## Most recent CI on the branch lineage

Commit `c5e5c6f6` ("Optimize macro save process", tip of `feat/coban-indicator`,
ancestor of `feat/esp32`), runs of 2026-02-01:

| Workflow | Run ID | Result | Failing jobs |
| --- | --- | --- | --- |
| Build | 21558251940 | failure | `build (xiao_ble, hummingbird)`; remaining matrix jobs `cancelled` |
| Tests | 21558251948 | failure | `run-tests (matrix-input)`; remaining jobs `cancelled` |
| BLE Tests | 21558251939 | failure | — |
| pre-commit | 21558251945 | failure | — |

Caveat: the large number of `cancelled` jobs indicates a concurrency
cancellation (a later push to the same branch), so this is indicative, not a
complete matrix. Job logs require authenticated access to retrieve.

## `main` @ `9ebbeff0a8b69a42f14aec022cdf16c7a107b9e0`

- No CI status recorded on the fork (`state: pending`, 0 statuses) — the
  fork's workflows do not run on `main` pushes for this commit.

## Implication for Phase 0 exit gate

The gate requires "current mainline builds and tests are green". As recorded
in `tests-2026-09-23.md`, the test suite does **not** build on the current
branch (pre-existing `indicator.c` failure). The gate is **not met** until
that fix lands (separate PR) and is re-verified.
